# FEC Schedule B/E bulk-substitution equivalence evidence status

Registered political-finance sources include credential-gated FEC API acquisition. The MAX certification line contains an explicit resumable Schedule B API producer that requires `FEC_API_KEY`; credential use is explicit and never discovered implicitly.

Candidate: FEC bulk data products as a possible substitute for Schedule B and/or Schedule E API retrieval.

No complete byte-backed comparison has yet proven semantic scope, temporal scope, row-universe equality, field mapping, selection equivalence, and aggregation equivalence between the bulk products and the API outputs required by the MoneySweep campaign-finance materialization contract.

Certification consequence: bulk FEC products MUST NOT be used to remove the credential dependency or satisfy campaign-finance gates unless a full equivalence proof passes.
