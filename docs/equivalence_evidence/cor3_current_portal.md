# COR3 equivalence evidence status

Registered source: `cor3`.

Candidate: current official COR3 / recovery.pr.gov public source.

Known public producer result retained from the certification line: `data/staging/processed/pr_cor3_projects.csv` had 0 data rows and SHA-256 `0c9eb91263e51edf80343e2d93ef4de37b770a3997b46604df344a6a784fabfa`.

The legacy authoritative workbook universe has known filenames/row counts/hashes, but the exact workbook bytes are not mounted in the current operator corpus. No semantic, temporal, row-universe, field-mapping, selection, or aggregation equivalence between the current public surface and the legacy workbook universe has been proven.

Certification consequence: current COR3 data MUST NOT substitute for the registered/frozen universe. State remains UNPROVEN until exact legacy bytes are recovered or a complete equivalence comparison is executed.
