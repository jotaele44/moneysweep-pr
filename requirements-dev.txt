# Development & CI tooling (lint, type-check, test). Runtime deps live in
# requirements.txt; this file is the single source for the quality-gate tooling
# referenced by .github/workflows/lint.yml, mypy.yml, tests.yml and
# .pre-commit-config.yaml. Keep ruff's version in sync with the pre-commit `rev`.
-r requirements.txt

ruff==0.15.18
mypy==2.1.0
pytest
pytest-cov

# Type stubs. `requests` ships no inline types; mypy emits import-untyped (which
# ignore_missing_imports does NOT suppress) unless these are installed, so they
# are required for the gating mypy check. Expand as more typed deps are added.
types-PyYAML
types-requests
